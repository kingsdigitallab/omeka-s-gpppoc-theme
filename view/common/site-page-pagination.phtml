<?php
$prevText = $this->translate('Prev');
$nextText = $this->translate('Next');
?>
<div class="site-page-pagination">
    <?php if ($prevPage) { echo $this->hyperlink($prevText, $prevPage->siteUrl()); } ?>
    <?php if ($nextPage) { echo $this->hyperlink($nextText, $nextPage->siteUrl()); } ?></span>
</div>
